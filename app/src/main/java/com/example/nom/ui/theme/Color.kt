package com.example.nom.ui.theme

import androidx.compose.ui.graphics.Color

val NomDarkBg = Color(0xFF0A1A0F)         // Primary background — deep forest black
val NomDarkSurface = Color(0xFF0F261A)    // Elevated surface — slightly lighter forest
val NomDarkCard = Color(0xFF132E1C)       // Card backgrounds — visible against surface
val NomGreenAccent = Color(0xFF4ADE80)    // Primary accent — buttons, active states, highlights
val NomGreenDark = Color(0xFF166534)      // Darker green — button pressed states, borders
val NomGreenSubtle = Color(0xFF22C55E)    // Medium green — progress bars, secondary accents
val NomTealSpirit = Color(0xFF2DD4BF)     // Spirit color — teal, used for Spirit-related UI
val NomGold = Color(0xFFFBBF24)           // Gold — achievements, legendary rarity, discovery banner
val NomRed = Color(0xFFF87171)            // Soft red — toxic warnings, danger states
val NomTextPrimary = Color(0xFFE8F5E9)    // Primary text — very light green-white
val NomTextSecondary = Color(0xFF94A3B8)  // Secondary text — muted blue-gray
val NomTextMuted = Color(0xFF64748B)      // Muted labels — timestamps, captions
val NomGlassBorder = Color(0xFF4ADE80).copy(alpha = 0.15f)  // Glass card border
val NomGlassFill = Color(0xFFFFFFFF).copy(alpha = 0.05f)    // Glass card fill
val NomGlassFillHover = Color(0xFFFFFFFF).copy(alpha = 0.08f) // Glass card elevated
